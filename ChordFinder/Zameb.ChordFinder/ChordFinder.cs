using NAudio.Midi;

namespace Zameb.ChordFinder
{
    public class ChordFinder
    {
        public IEnumerable<Chord> GetChords(MidiEventCollection events)
        {
            return new List<Chord>();
        }
    }
}
